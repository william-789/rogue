class Score {
  description;
  points;
  type;
  constructor(description, points, type) {
    this.description = description;
    this.points = points;
    this.type = type;
  }
}

export default Score;